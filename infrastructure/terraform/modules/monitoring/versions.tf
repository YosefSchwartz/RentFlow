# Monitoring module — provider requirements.
# Provider CONFIGURATION lives in the calling environment root, never here.

terraform {
  required_version = ">= 1.6.0"

  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.60"
    }
  }
}
